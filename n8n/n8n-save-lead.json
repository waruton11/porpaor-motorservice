{
  "name": "POST save-lead",
  "nodes": [
    {
      "id": "lead-001",
      "name": "POST save-lead",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        240,
        300
      ],
      "webhookId": "save-lead",
      "parameters": {
        "httpMethod": "POST",
        "path": "save-lead",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "lead-002",
      "name": "Build SQL",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        460,
        300
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const b = $input.first().json.body || $input.first().json;\nconst esc = (s) => (s == null || s === '') ? 'NULL' : `'${String(s).replace(/'/g, \"''\")}'`;\nconst num = (n) => (n == null || n === '' || isNaN(Number(n))) ? 'NULL' : Number(n);\nconst lit = (s) => String(s == null ? '' : s).replace(/'/g, \"''\");\nconst detail = JSON.stringify(b.detail || {}).replace(/'/g, \"''\");\n\nconst insertLead = `INSERT INTO leads (lead_type, model, variant, color, pay_type, down_pct, installment, period, price, cust_name, cust_phone, line_user_id, detail)\nVALUES (${esc(b.lead_type)}, ${esc(b.model)}, ${esc(b.variant)}, ${esc(b.color)}, ${esc(b.pay_type)}, ${num(b.down_pct)}, ${num(b.installment)}, ${num(b.period)}, ${num(b.price)}, ${esc(b.cust_name)}, ${esc(b.cust_phone)}, ${esc(b.line_user_id)}, '${detail}'::jsonb);`;\n\nlet interest = b.model || '';\nif (b.lead_type === 'tradein' && b.detail && b.detail.new_model) interest = b.detail.new_model;\nconst uid = lit(b.line_user_id);\nlet upsertCust = '';\nif (uid) {\n  upsertCust = `INSERT INTO customers (line_user_id, display_name, phone, interest_model, lead_count, last_seen, updated_at)\nVALUES ('${uid}', ${esc(b.cust_name)}, ${esc(b.cust_phone)}, ${esc(interest)}, 1, NOW(), NOW())\nON CONFLICT (line_user_id) DO UPDATE SET\n  display_name = COALESCE(customers.display_name, NULLIF(EXCLUDED.display_name, '')),\n  phone = COALESCE(NULLIF(EXCLUDED.phone, ''), customers.phone),\n  interest_model = COALESCE(NULLIF(EXCLUDED.interest_model, ''), customers.interest_model),\n  lead_count = customers.lead_count + 1,\n  last_seen = NOW(),\n  updated_at = NOW()\nRETURNING id;`;\n}\n\nreturn [{ json: { sql: insertLead + '\\n' + upsertCust, lead: b } }];"
      }
    },
    {
      "id": "lead-003",
      "name": "Insert",
      "type": "n8n-nodes-base.postgres",
      "typeVersion": 2.5,
      "position": [
        680,
        300
      ],
      "parameters": {
        "operation": "executeQuery",
        "query": "={{ $json.sql }}",
        "options": {}
      },
      "credentials": {
        "postgres": {
          "name": "Postgres account"
        }
      }
    },
    {
      "id": "lead-004",
      "name": "Respond OK",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        1340,
        300
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ ok: true }) }}",
        "options": {}
      }
    },
    {
      "id": "lead-005",
      "name": "Build Sales Card",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        900,
        460
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// ====== CONFIG: เอา token + group id มาวางตรงนี้ ======\nconst SALES_TOKEN = 'SALES_TOKEN_HERE';\nconst SALES_GROUP_ID = 'SALES_GROUP_ID_HERE';\n\nconst L = $('Build SQL').first().json.lead || {};\nlet custId = null;\ntry { custId = $('Insert').first().json.id; } catch (e) {}\nconst code = custId ? ('PP-' + String(custId).padStart(4, '0')) : 'PP-????';\nconst uid = L.line_user_id || '';\n\nconst TYPE = {\n  installment: { label: 'ผ่อน', color: '#1eaa5c', emoji: '🟢' },\n  cash:        { label: 'เงินสด', color: '#1eaa5c', emoji: '🟢' },\n  tradein:     { label: 'เทิร์นรถเก่า', color: '#f57c00', emoji: '🟠' },\n  testride:    { label: 'ทดลองขับ', color: '#2563eb', emoji: '🔵' }\n};\nconst t = TYPE[L.lead_type] || { label: L.lead_type || 'lead', color: '#666666', emoji: '🔔' };\nconst baht = (n) => '฿' + Number(n || 0).toLocaleString('en-US');\n\nconst lines = [];\nif (L.lead_type === 'installment') {\n  lines.push('💳 ค่างวด ' + baht(L.installment) + ' x ' + (L.period || '-') + ' ด.');\n  if (L.down_pct != null && L.down_pct !== '') lines.push('เงินดาวน์ ' + L.down_pct + '%');\n} else if (L.lead_type === 'cash') {\n  lines.push('💵 เงินสด ' + baht(L.price));\n} else if (L.lead_type === 'tradein') {\n  lines.push('🔄 รถเทิร์น: ' + (L.model || '-'));\n  const nm = (L.detail && L.detail.new_model) || '';\n  if (nm) lines.push('อยากได้: ' + nm);\n} else if (L.lead_type === 'testride') {\n  lines.push('🏍️ ลองรุ่น: ' + (L.model || '-'));\n  const dt = (L.detail && L.detail.datetime) || (L.detail && L.detail.date) || '';\n  if (dt) lines.push('📅 ' + dt);\n}\n\nconst variantColor = [L.variant, L.color].filter(Boolean).join(' · ');\nconst phone = L.cust_phone ? ('📞 ' + L.cust_phone) : '📞 (รอลูกค้าฝากเบอร์)';\n\nconst body = [\n  { type: 'box', layout: 'vertical', backgroundColor: t.color, cornerRadius: '6px', paddingAll: '8px',\n    contents: [{ type: 'text', text: t.emoji + ' ลูกค้าใหม่ · ' + t.label, color: '#ffffff', weight: 'bold', size: 'sm' }] },\n  { type: 'text', text: code, weight: 'bold', size: 'lg', margin: 'md' },\n  { type: 'text', text: (L.model || '-'), size: 'md', wrap: true }\n];\nif (variantColor) body.push({ type: 'text', text: variantColor, size: 'sm', color: '#888888' });\nlines.forEach(d => body.push({ type: 'text', text: d, size: 'sm', wrap: true, margin: 'sm' }));\nbody.push({ type: 'text', text: phone, size: 'sm', margin: 'sm' });\nbody.push({ type: 'text', text: '🔴 รอติดต่อ', size: 'sm', weight: 'bold', color: '#cc0000', margin: 'sm' });\n\nconst flex = {\n  type: 'flex', altText: 'ลูกค้าใหม่ ' + code + ' (' + t.label + ')',\n  contents: {\n    type: 'bubble',\n    body: { type: 'box', layout: 'vertical', spacing: 'sm', contents: body },\n    footer: { type: 'box', layout: 'vertical', spacing: 'sm', contents: [\n      { type: 'button', style: 'primary', color: t.color, height: 'sm',\n        action: { type: 'postback', label: '📞 รับเคส', data: 'action=claim&cid=' + uid, displayText: 'รับเคส ' + code } },\n      { type: 'box', layout: 'horizontal', spacing: 'sm', contents: [\n        { type: 'button', style: 'secondary', height: 'sm',\n          action: { type: 'postback', label: '✅ อนุมัติ', data: 'action=approve&cid=' + uid, displayText: 'อนุมัติ ' + code } },\n        { type: 'button', style: 'secondary', height: 'sm',\n          action: { type: 'postback', label: '❌ ไม่ผ่าน', data: 'action=reject&cid=' + uid, displayText: 'ไม่ผ่าน ' + code } }\n      ]}\n    ]}\n  }\n};\n\nconst ready = SALES_GROUP_ID && SALES_GROUP_ID !== 'SALES_GROUP_ID_HERE' && SALES_TOKEN !== 'SALES_TOKEN_HERE';\nreturn [{ json: { to: SALES_GROUP_ID, messages: [flex], token: SALES_TOKEN, ready: ready } }];"
      }
    },
    {
      "id": "lead-006",
      "name": "Push to Sales Group",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1120,
        460
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "method": "POST",
        "url": "https://api.line.me/v2/bot/message/push",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "={{ 'Bearer ' + $json.token }}"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{ JSON.stringify({ to: $json.to, messages: $json.messages }) }}",
        "options": {}
      }
    }
  ],
  "connections": {
    "POST save-lead": {
      "main": [
        [
          {
            "node": "Build SQL",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build SQL": {
      "main": [
        [
          {
            "node": "Insert",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Insert": {
      "main": [
        [
          {
            "node": "Build Sales Card",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Sales Card": {
      "main": [
        [
          {
            "node": "Push to Sales Group",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Push to Sales Group": {
      "main": [
        [
          {
            "node": "Respond OK",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": true,
  "settings": {
    "executionOrder": "v1"
  }
}